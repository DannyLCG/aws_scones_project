# Fixtures

The per-Lambda JSON test events currently in `scripts/LAMBDA/*/*.json`
(`s3_input_image.json`, `imageSerializerOutput.json`,
`predictImageLabelOutput.json`) move here once the corresponding
`tests/unit/test_*.py` files are written against `src/lambdas/*/handler.py`.
