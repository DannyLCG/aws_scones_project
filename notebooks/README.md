# Notebooks

`demo.ipynb` will live here — a thin walkthrough that imports from `src/`
(`src.etl`, `src.training`, `src.monitoring`) instead of owning ETL, training,
or monitoring logic directly the way `starter.ipynb` does today.
